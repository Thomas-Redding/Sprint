class List
	List List()
		int x = 5
		int z = (5+2)*3
		int y = 8 and 9 or 5 * 3 + 2
		int arr = [2, 5, 9]
		int dic = {1: 2, 3: 4, 5: 6}
	void push(V item)
	void insert(uint index, V item)
	V pop()
	V remove(uint index)
	V at(uint index)
		return arr[index]

	void clear()
		length = 0

	void isEmpty()
		return length == 0

	readonly V* arr
	readonly uint length = 0

	private uint capacity = 20
	private void expand()

/*


*/